{  
  "name": "proxy-rotator",  
  "version": "1.0.0",  
  "description": "Rotating proxy server",  
  "main": "server.js",  
  "scripts": {  
    "start": "node server.js"  
  },  
  "dependencies": {  
    "express": "^4.18.2",  
    "express-basic-auth": "^1.2.1",  
    "http-proxy-middleware": "^2.0.6"  
  },  
  "engines": {  
    "node": ">=18.0.0"  
  }  
}
